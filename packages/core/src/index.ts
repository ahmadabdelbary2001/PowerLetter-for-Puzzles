// @powerletter/core — Shared game logic, domain models, and stores

// ── Types ────────────────────────────────────────────────────────
export type {
  Language,
  GameMode,
  GameType,
  Difficulty,
  AgeGroup,
  GameCategory,
  GameId,
  GameLevel,
  IGameEngine,
  Team,
  Scores,
  GameState,
  GameRegistryEntry,
  LessonType,
  AnimalLesson,
  CountryLesson,
  VocabularyLesson,
  Lesson,
} from "./types/game";

// ── Game Registry (Phase 5 will add the full implementation) ─────
// Re-exported from here when ready
