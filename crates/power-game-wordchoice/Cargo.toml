[package]
name = "power-game-wordchoice"
version = "1.0.1"
edition = "2021"
authors = ["PowerLetter Team"]
description = "Word Choice game logic for PowerLetter"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
