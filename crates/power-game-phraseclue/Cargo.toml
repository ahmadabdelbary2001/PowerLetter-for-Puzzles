[package]
name = "power-game-phraseclue"
version = "1.0.8"
edition = "2021"
authors = ["PowerLetter Team"]
description = "Phrase Clue game logic for PowerLetter"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
