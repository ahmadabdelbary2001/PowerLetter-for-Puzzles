# Changesets

Hello and welcome! This folder has been automatically generated by `@changesets/cli`, a build tool that works
with multi-package repos, or single-package repos to help you version and publish your code.

## Usage

1. **Add a changeset** (when you make a change):
   ```bash
   pnpm changeset
   ```

2. **Version packages** (before release):
   ```bash
   pnpm version-packages
   ```

3. **Publish** (after versioning):
   ```bash
   pnpm release
   ```

More info at [changesets.dev](https://changesets.dev)
